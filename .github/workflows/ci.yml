name: Regression Test

on:
  push:
    branches: [ "main", "devel*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  Test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
    
    - run: npm ci
